export type Sp143RokConfig = {
  timezone: string;
  schoolSite: string;
  calendarPage: string;
  fallbackDocx: string;
};

export type SchoolYearBounds = {
  start: string;
  end: string;
  label: string;
};
